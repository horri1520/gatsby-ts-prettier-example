import React from 'react';
import { PageRoot } from '../../../styles/styled-components';

const IndexTemplate: React.FC = () => {
  return <PageRoot>IndexTemplate</PageRoot>;
};

export default IndexTemplate;
