import React from 'react';
import { PageRoot } from '../../../styles/styled-components';

const NotFoundTemplate: React.FC = () => {
  return <PageRoot>404 Not found.</PageRoot>;
};

export default NotFoundTemplate;
